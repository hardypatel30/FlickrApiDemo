<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="model" type="com.example.ridecellpracticaldemo.responsemodels.Photo"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">


        <ImageView android:id="@+id/imgMainPhoto"
                   android:layout_width="100dp"
                   android:layout_height="100dp"
                   app:mainImage="@{model.urlO}"
                   app:layout_constraintTop_toTopOf="parent"
                   app:layout_constraintLeft_toLeftOf="parent"/>

        <androidx.appcompat.widget.AppCompatTextView
                android:id="@+id/txtMain"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{model.title}"
                app:layout_constraintTop_toTopOf="@+id/imgMainPhoto"
                app:layout_constraintLeft_toRightOf="@+id/imgMainPhoto"/>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>